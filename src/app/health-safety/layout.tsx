// Health & Safety layout - uses root layout header
import type { ReactNode } from "react";

export default function HealthSafetyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
